import { authStore } from './AuthStore';
import { clientStore } from './ClientStore';
import { uiStore } from './UIStore';

// For convenience, though individual imports work fine too.
export { authStore, clientStore, uiStore };
